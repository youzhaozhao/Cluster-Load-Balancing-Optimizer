# Cluster-Load-Balancing-Optimizer
A hybrid optimization system for server cluster load balancing under capacity and bandwidth constraints, combining Floyd–Warshall, greedy initialization, simulated annealing, and discrete-event migration simulation.
